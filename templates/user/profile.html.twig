{% extends 'base.html.twig' %}

{% block title %}

{% endblock %}

{% block main %}

    <h3>{{ user.username }}</h3>

    <h4>Мои публикации</h4>

    {% for post in posts %}

        <div class="md-post">
            {{ post.title }} {{ postStatus(post.status) }}
        </div>

    {% endfor %}

{% endblock %}
