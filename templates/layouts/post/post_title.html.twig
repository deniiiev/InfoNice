{% if link %}
        <h3>
        <a href="{{ path('post_show', {'id':post.id}) }}">
                {{ post.title }}
        </a>
        </h3>
{% else %}
        <h1>
                {{ post.title }}
        </h1>
{% endif %}