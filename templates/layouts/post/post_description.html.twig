{% if post.description %}
    <p class="content">
        {% if text is null %}
            {{ post.description | raw }}
        {% else %}
            {{ post.description | raw | slice(0, text) }}..
        {% endif %}
    </p>
{% endif %}
