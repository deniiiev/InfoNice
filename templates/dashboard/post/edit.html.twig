{% extends 'dashboard/base.html.twig' %}

{% block title %}{{ (post.section ~ '.editing') | trans({},'posts') }}{% endblock %}

{% block body %}

    <div class="md-box md-content-padding">

        <h1>{{ (post.section ~ '.editing') | trans({},'posts') }}</h1>

        {{ form_start(form) }}

            {{ form_widget(form) }}
            <button class="btn btn-primary">{{ button_label|default('Сохранить') }}</button>
            <a class="btn btn-link float-right" href="{{ path('dash_post_index') }}">
                Отмена
            </a>

        {{ form_end(form) }}

    </div>

{% endblock %}

{% block scripts %}
    {{ chosen() }}
    {{ ckeditor() }}
{% endblock %}
